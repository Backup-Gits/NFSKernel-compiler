version: 2.1
jobs:
  compile:
   docker:
      - image: xyzuan/xdocker:latest
   environment:
      GIT_EMAIL: jarbull86@gmail.com
      GIT_NAME: AnGgIt86
      VERSION: 2.2
   steps:
      - run:
          command: |
           apt-get -y update && apt-get -y upgrade
           dpkg-reconfigure --frontend noninteractive tzdata
           curl https://raw.githubusercontent.com/AnGgIt866/test/main/llvm.sh -o llvm.sh
           source ./llvm.sh
workflows:
  version: 2.1
  cooking:
    jobs:
    - compile
